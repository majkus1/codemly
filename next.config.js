/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      defaultLocale: 'en',
      locales: ['pl', 'en'],
      localeDetection: false,
    },
    // Możesz dodać tutaj inne konfiguracje, jeśli są potrzebne
  };
  
  module.exports = nextConfig;
  